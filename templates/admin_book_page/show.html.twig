{% extends 'base-backend.html.twig' %}

{% block title %}Page de livre{% endblock %}

{% block body %}
<a href="{{ path('app_admin_book_page_index') }}" class="btn btn-dark btn-sm">Retour à la liste</a>
<h1 class= "mt-3">Page de livre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_page.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ book_page.imageName }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ book_page.updatedAt ? book_page.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex justify-content align-items-center">
<a href="{{ path('app_admin_book_page_edit', {'id': book_page.id}) }}" class="btn btn-success mx-1"><i class="align-middle" data-feather="edit-2"></i></a>
    {{ include('admin_book_page/_delete_form.html.twig') }}
{% endblock %}
