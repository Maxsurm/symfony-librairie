{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    {{ form_row(form.isActive) }}
    <div class="d-flex flex-wrap">
        <div class="col-12 col-md-6 px-3">
        {{ form_label(form.titre) }}
        {{ form_widget(form.titre) }}
        {{ form_help(form.titre) }}
        </div>
        <div class="col-12 col-md-6 px-3">
        {{ form_row(form.signature) }}
        </div>
    </div>
    <div class="px-3">{{ form_row(form.texte) }}</div>
    {% include 'admin_common/_required-fields.html.twig'%}
    <button class="btn btn-success mt-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}