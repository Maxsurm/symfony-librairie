{% extends 'base-backend.html.twig' %}

{% block title %}Genre de Livre{% endblock %}

{% block body %}
<a href="{{ path('app_admin_type_of_book_index') }}" class="btn btn-dark btn-sm">Retour à la liste</a>
<h1 class= "mt-3">Genre de Livre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_book.id }}</td>
            </tr>
            <tr>
                <th>TypeName</th>
                <td>{{ type_of_book.typeName }}</td>
            </tr>
            <tr>
                <th>TypeDes</th>
                <td>{{ type_of_book.typeDes }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex justify-content align-items-center">
<a href="{{ path('app_admin_type_of_book_edit', {'id': type_of_book.id}) }}" class="btn btn-success mx-1"><i class="align-middle" data-feather="edit-2"></i></a>
    {{ include('admin_type_of_book/_delete_form.html.twig') }}
</div>
{% endblock %}
