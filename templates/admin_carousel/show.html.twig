{% extends 'base-backend.html.twig' %}

{% block title %}Carousel{% endblock %}

{% block body %}
<a href="{{ path('app_admin_carousel_index') }}" class="btn btn-dark btn-sm">Retour à la liste</a>
<h1 class= "mt-3">Carousel</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carousel.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ carousel.imageName }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ carousel.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ carousel.text }}</td>
            </tr>
            <tr>
                <th>Tag</th>
                <td>{{ carousel.tag }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex justify-content align-items-center">
<a href="{{ path('app_admin_carousel_edit', {'id': carousel.id}) }}" class="btn btn-success mx-1"><i class="align-middle" data-feather="edit-2"></i></a>
    {{ include('admin_carousel/_delete_form.html.twig') }}
</div>
{% endblock %}
